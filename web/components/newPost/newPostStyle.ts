import { Theme } from '@material-ui/core';

export default (theme: Theme) => ({
  editorDiv: {
    width: '90%',
    margin: '0 auto',
  },
  buttonDiv: {
    display: 'flex',
    justifyContent: 'flex-end',
  },
});
