input CreateThreadInput {
  text: String!
  title: String!
  forumId: Int!
}

type Mutation {
  threadCreate(input: CreateThreadInput!): CreateThreadResponse!
}

type CreateThreadResponse {
  id: Int!
  title: String!
}
