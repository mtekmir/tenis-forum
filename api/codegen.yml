schema: http://localhost:5000
overwrite: true
config:
  contextType: ./types#IContext
generates:
  ./src/types/schema.d.ts:
    plugins:
      - add
      - time
      - typescript-common
      - typescript-server
      - typescript-resolvers
