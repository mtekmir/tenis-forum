overwrite: true
schema: http://localhost:5000/graphql
config:
  contextType: ./types#IContext
generates:
  src/types/schema.ts:
    plugins:
      - add: '/* tslint:disable */'
      - time
      - typescript-common
      - typescript-server
      - typescript-resolvers
