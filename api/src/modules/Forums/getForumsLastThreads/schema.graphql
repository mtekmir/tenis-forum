type Query {
  forumGetLastThreads: [LastThreadOfForum!]!
}

type LastThreadOfForum {
  id: Int!
  createdAt: Date!
  title: String!
  forumId: Int!
  ownerId: String!
  profileImageUrl: String!
  username: String!
}
