type Query {
  threadGet(input: GetThreadInput!): GetThreadResponse!
}

input GetThreadInput {
  id: String!
  offset: Int
  limit: Int
}

type GetThreadResponse {
  thread: Thread!
  postCount: Int!
}
