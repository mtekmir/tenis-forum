enum UserPermissions {
  ADMIN
  USER
}

type User {
  id: String!
  username: String!
  email: String!
  permissions: [UserPermissions!]!
  signature: String
  profileImageUrl: String
  profile: UserProfile
}
